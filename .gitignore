# Local data — the SEM micrographs and annotations live outside the repo
segmentations/
Segmentations_VB/
bead_data.zip

# Pipeline scratch: prepared images, fold manifests, model predictions.
# All regenerable from the raw data by code/prepare_data.py.
work/
work_smoke/
results_smoke/

# results/ IS tracked. Chapter 5 \inputs the generated tables from it, and
# metrics.json is the provenance for every number in the thesis: it records which
# run produced each figure. Both must travel with the LaTeX sources.

# Claude Code project instructions
CLAUDE.md

# ---- LaTeX build artefacts (the .tex sources ARE tracked) ----
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.synctex.gz
*.toc
*.idx
*.ilg
*.ind
thesis.pdf

# ---- Python ----
__pycache__/
*.pyc
.venv/
venv/
