# Local data — the SEM micrographs and annotations live outside the repo
segmentations/
Segmentations_VB/
bead_data.zip

# Pipeline scratch: prepared images, fold manifests, model predictions.
# All regenerable from the raw data by code/prepare_data.py.
work/

# Measured metrics and the tables generated from them. Untracked on purpose:
# a partial run left in the repository is indistinguishable from a final one.
# The definitive numbers are the tables pasted into chapters/chapter5.tex.
results/

# Claude Code project instructions
CLAUDE.md

# ---- LaTeX build artefacts (the .tex sources ARE tracked) ----
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.synctex.gz
*.toc
*.idx
*.ilg
*.ind
thesis.pdf

# ---- Python ----
__pycache__/
*.pyc
.venv/
venv/
