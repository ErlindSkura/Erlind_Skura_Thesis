\chapter{RESULTS AND DISCUSSION}
\label{ch:results}

% =====================================================================
% STATUS: SCAFFOLD -- THE TRAINING RUNS HAVE NOT BEEN EXECUTED YET.
%
% Every table below is empty by design. Do not fill any cell with an
% estimated, expected or illustrative number: a plausible-looking placeholder
% that survives into the submitted document is indistinguishable from
% fabricated data.
%
% Nor should these tables be filled in by hand once the runs exist.
% code/make_tables.py reads results/metrics.json and emits every table in this
% chapter, already formatted, into results/chapter5_tables.tex. Replace the
% empty tables below with that file's contents, or \input it. Generating them
% means no number can reach the thesis without a run having produced it, and
% re-running the pipeline updates the chapter rather than silently
% contradicting it.
%
% The section structure follows the objectives set out in Section 1.4, so
% that each result answers a stated question.
% =====================================================================

\section{Introduction}

This chapter reports the experimental results. It first establishes the dataset
characterisation and calibration, which are complete, and then reports the
segmentation experiments under the leave-one-specimen-out protocol of
Section~\ref{sec:protocol}.

\section{Dataset Characterisation}

The descriptive analysis of the dataset is complete and is reported in
Chapter~\ref{ch:methodology}: 606 annotated beads across 11 micrographs of four
specimens, at three magnifications, with the pixel calibration of
Table~\ref{tab:calibration}.

The first objective of Section~\ref{sec:objectives} is met. The median equivalent
bead diameter is
7.92\,$\mu$m at $500\times$, 7.34\,$\mu$m at $1000\times$ and 6.75\,$\mu$m at
$3000\times$, an agreement of within 15\% against a stated criterion of 20\%. Since
the three magnifications image the same four specimens, this agreement is evidence
both that the scale calibration is correct and that the annotation is internally
consistent across magnifications -- an annotator who applied a different size
threshold at different magnifications would have broken it.

The residual downward trend with magnification is consistent with border clipping
rather than calibration error. At $3000\times$ the field is 94.8\,$\mu$m wide, so a
bead of 8\,$\mu$m occupies a much larger fraction of it and is correspondingly more
likely to intersect the image boundary and be excluded from annotation. Only 52 of
the 606 beads come from $3000\times$ micrographs, so this group is also the noisiest.

\section{Experimental Setup}

% TODO: complete once training has been run. Report the exact configuration used,
% not the configuration planned in Table 4.1.

\subsection{Training Configuration}

\subsection{Fold Composition}

\begin{table}[htbp]
    \centering
    \caption{Leave-one-specimen-out fold composition. Bead counts are known from the
    annotation and are filled in; results columns are added once the runs complete.}
    \label{tab:folds}
    \begin{tabular}{llrr}
        \toprule
        Fold & Test specimen & Test micrographs & Test beads \\
        \midrule
        1 & Z2 & 3 & 132 \\
        2 & Z4 & 2 & 63  \\
        3 & Z5 & 3 & 173 \\
        4 & Z6 & 3 & 238 \\
        \midrule
        \multicolumn{2}{l}{\textbf{Total}} & \textbf{11} & \textbf{606} \\
        \bottomrule
    \end{tabular}
\end{table}

The folds are markedly unbalanced: specimen Z4 contributes only 63 beads across two
micrographs, while Z6 contributes 238 across three. Per-fold results are therefore
reported individually as well as averaged, since a mean over four folds of this
size is not a stable summary on its own.

\section{Segmentation Results}

\subsection{Mask R-CNN}

\begin{table}[htbp]
    \centering
    \caption{Mask R-CNN under leave-one-specimen-out cross-validation.}
    \label{tab:maskrcnn_results}
    \begin{tabular}{lrrrr}
        \toprule
        Test specimen & $\mathrm{AP}_{50}$ & $\mathrm{AP}_{75}$ & AP & AJI \\
        \midrule
        Z2 & & & & \\
        Z4 & & & & \\
        Z5 & & & & \\
        Z6 & & & & \\
        \midrule
        Mean $\pm$ s.d. & & & & \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{U-Net Semantic Baseline}

\subsection{Classical Baseline}

\subsection{Method Comparison}

\begin{table}[htbp]
    \centering
    \caption{The three methods under an identical protocol, averaged over the four
    leave-one-specimen-out folds.}
    \label{tab:method_comparison}
    \begin{tabular}{lrrrr}
        \toprule
        Method & $\mathrm{AP}_{50}$ & AJI & Counting error (\%) & Merge rate (\%) \\
        \midrule
        Otsu + watershed & & & & \\
        U-Net + CC       & & & & \\
        Mask R-CNN       & & & & \\
        \bottomrule
    \end{tabular}
\end{table}

\section{The Cost of a Naive Split}

% This is one of the stated contributions. Run the same model under both protocols
% and report the difference. Keep both rows even if the gap is small -- a small gap
% is itself a reportable result.

\begin{table}[htbp]
    \centering
    \caption{The same model evaluated under a specimen-wise protocol and under a
    random split over the 11 micrographs. The difference is the amount by which a
    naive protocol would have overstated performance on this dataset.}
    \label{tab:leakage}
    \begin{tabular}{lrrr}
        \toprule
        Protocol & $\mathrm{AP}_{50}$ & AJI & Counting error (\%) \\
        \midrule
        Random split over images & & & \\
        Leave-one-specimen-out   & & & \\
        \midrule
        Difference & & & \\
        \bottomrule
    \end{tabular}
\end{table}

\section{Behaviour Across Magnification}

% Report per-magnification breakdown. The central question is whether one model
% handles all three, or whether performance collapses at 3000x where only 52 beads
% are available.

\section{Physical Quantification}

\subsection{Bead Count and Areal Density}

\subsection{Size Distribution Agreement}

% Compare predicted vs manual distributions of equivalent diameter, by median, IQR
% and a two-sample KS test. This is the result that determines whether the method
% is usable for process optimisation.

\section{Discussion}

\subsection{Key Findings}

\subsection{Comparison with the Literature}

\subsection{Limitations}
\label{sec:limitations}

The limitations below follow from the dataset and are known already; they should be
stated regardless of how the experiments turn out.

\textbf{Four specimens.} The strongest claim this dataset can support is
generalisation across four specimens of what appears to be one material system. It
cannot establish generalisation to a different polymer, a different solvent or a
different instrument.

\textbf{Fifty-two beads at $3000\times$.} The highest magnification is represented by
only 52 annotated objects, so per-magnification results for $3000\times$ rest on a
small sample and should be read as indicative.

\textbf{One annotator.} All 606 annotations come from a single source, so
annotator bias cannot be distinguished from ground truth, and no inter-annotator
agreement can be reported.
% TODO(supervisor): if a subset can be re-annotated by a second person, this
% limitation can be converted into a measurement instead. See question D4.

\textbf{Single class.} The annotation does not distinguish beads from locally
thickened fibre segments, so the boundary between the two is defined implicitly by
the annotator's judgement rather than by a stated criterion.

\subsection{Reliability of the Reported Numbers}

% State plainly: how many runs per configuration, whether seeds were varied, what
% the spread across folds was, and which numbers are single-run point estimates.

\section{Summary}
